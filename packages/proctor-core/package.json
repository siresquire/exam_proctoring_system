{
  "name": "@proctor/core",
  "version": "0.0.1",
  "private": true,
  "description": "Framework-agnostic proctoring engine shared by System 1 (Forms wrapper) and System 2 (platform exam room).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "happy-dom": "^15.11.7",
    "typescript": "^5",
    "vitest": "^2.1.8"
  }
}

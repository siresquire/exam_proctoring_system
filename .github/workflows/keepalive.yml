name: Keepalive

# Supabase free-tier projects pause after 7 days of inactivity. Ping every 3
# days to stay well inside that window, and again to pre-warm before exam
# windows (Phase 6). Part 2 (Supabase layer) wires the real ping in.
on:
  schedule:
    - cron: "0 6 */3 * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          echo "TODO: ping Supabase"
